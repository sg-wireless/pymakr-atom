exports.error = (name, err) => {
  // console.error(name,' | ',err.toString()); // to use for debugging
};

exports.log = (name, info) => {
  // console.log(name,' | ',info.toString()); // to use for debugging
};
